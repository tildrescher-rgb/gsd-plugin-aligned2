{
  "description": "GSD workflow hooks -- session context loading, workflow enforcement, and tool-use monitoring. Hook commands use an inline Node resolver so they fall back to the newest cached plugin version when the session's baked ${CLAUDE_PLUGIN_ROOT} path has been pruned after an upgrade.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const f=require('fs'),p=require('path'),o=require('os'),c=[];if(process.env.CLAUDE_PLUGIN_ROOT)c.push(p.join(process.env.CLAUDE_PLUGIN_ROOT,'bin/gsd-tools.cjs'));const d=p.join(o.homedir(),'.claude/plugins/cache/gsd-plugin/gsd');try{for(const v of f.readdirSync(d).filter(x=>/^\\d+\\.\\d+\\.\\d+$/.test(x)).sort((a,b)=>{const A=a.split('.').map(Number),B=b.split('.').map(Number);return B[0]-A[0]||B[1]-A[1]||B[2]-A[2];}))c.push(p.join(d,v,'bin/gsd-tools.cjs'));}catch(e){}for(const x of c){if(!f.existsSync(x))continue;if(process.env.CLAUDE_PLUGIN_ROOT&&x!==c[0])process.stderr.write('GSD: plugin path stale, using '+x+'\\n');process.argv.splice(1,0,x);require(x);break;}\" hook session-start",
            "timeout": 5000
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const f=require('fs'),p=require('path'),o=require('os'),c=[];if(process.env.CLAUDE_PLUGIN_ROOT)c.push(p.join(process.env.CLAUDE_PLUGIN_ROOT,'bin/gsd-tools.cjs'));const d=p.join(o.homedir(),'.claude/plugins/cache/gsd-plugin/gsd');try{for(const v of f.readdirSync(d).filter(x=>/^\\d+\\.\\d+\\.\\d+$/.test(x)).sort((a,b)=>{const A=a.split('.').map(Number),B=b.split('.').map(Number);return B[0]-A[0]||B[1]-A[1]||B[2]-A[2];}))c.push(p.join(d,v,'bin/gsd-tools.cjs'));}catch(e){}for(const x of c){if(!f.existsSync(x))continue;if(process.env.CLAUDE_PLUGIN_ROOT&&x!==c[0])process.stderr.write('GSD: plugin path stale, using '+x+'\\n');process.argv.splice(1,0,x);require(x);break;}\" hook pre-tool-use",
            "timeout": 3000
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const f=require('fs'),p=require('path'),o=require('os'),c=[];if(process.env.CLAUDE_PLUGIN_ROOT)c.push(p.join(process.env.CLAUDE_PLUGIN_ROOT,'bin/gsd-tools.cjs'));const d=p.join(o.homedir(),'.claude/plugins/cache/gsd-plugin/gsd');try{for(const v of f.readdirSync(d).filter(x=>/^\\d+\\.\\d+\\.\\d+$/.test(x)).sort((a,b)=>{const A=a.split('.').map(Number),B=b.split('.').map(Number);return B[0]-A[0]||B[1]-A[1]||B[2]-A[2];}))c.push(p.join(d,v,'bin/gsd-tools.cjs'));}catch(e){}for(const x of c){if(!f.existsSync(x))continue;if(process.env.CLAUDE_PLUGIN_ROOT&&x!==c[0])process.stderr.write('GSD: plugin path stale, using '+x+'\\n');process.argv.splice(1,0,x);require(x);break;}\" hook post-tool-use",
            "timeout": 3000
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const f=require('fs'),p=require('path'),o=require('os'),c=[];if(process.env.CLAUDE_PLUGIN_ROOT)c.push(p.join(process.env.CLAUDE_PLUGIN_ROOT,'bin/gsd-tools.cjs'));const d=p.join(o.homedir(),'.claude/plugins/cache/gsd-plugin/gsd');try{for(const v of f.readdirSync(d).filter(x=>/^\\d+\\.\\d+\\.\\d+$/.test(x)).sort((a,b)=>{const A=a.split('.').map(Number),B=b.split('.').map(Number);return B[0]-A[0]||B[1]-A[1]||B[2]-A[2];}))c.push(p.join(d,v,'bin/gsd-tools.cjs'));}catch(e){}for(const x of c){if(!f.existsSync(x))continue;if(process.env.CLAUDE_PLUGIN_ROOT&&x!==c[0])process.stderr.write('GSD: plugin path stale, using '+x+'\\n');process.argv.splice(1,0,x);require(x);break;}\" hook pre-compact",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
